node_modules/
dist-preview/
.env
*.local
.DS_Store

# local runtime artifacts
*.pid
preview.log
preview.err.log
bridge/daemon-hour.out
bridge/mock-*.log

# editor-local config (CodeGPT extension key etc.) — machine-specific
.vscode/
# Freebuff preview thread state + run notes — tool-local
.freebuff/
# runtime mirror regenerated by every build (build script syncs it; app fetches with no-store)
/bridge-memory-log.json
